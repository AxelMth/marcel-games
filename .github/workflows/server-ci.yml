name: Server CI & Deploy

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "server/**"
  pull_request:
    paths:
      - "server/**"

jobs:
  build-test-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.23.0"

      - name: Download Go modules
        working-directory: server
        run: go mod download

      - name: Generate Prisma client
        working-directory: server
        run: go run github.com/steebchen/prisma-client-go generate

      - name: Build API binary
        working-directory: server
        run: go build ./cmd/api

      - name: Run Go tests
        working-directory: server
        run: go test ./...

      - name: Set up Flyctl
        if: github.event_name == 'push'
        uses: superfly/flyctl-actions/setup-flyctl@master

      - name: Deploy Earthunt API
        if: github.event_name == 'push'
        working-directory: server
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_EARTHUNT_API_TOKEN }}
        run: flyctl deploy --config fly.earthunt.toml --remote-only

      - name: Deploy Wordclimb API
        if: github.event_name == 'push'
        working-directory: server
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_WORDCLIMB_API_TOKEN }}
        run: flyctl deploy --config fly.wordclimb.toml --remote-only

