node_modules
.pnpm-store

# Keep environment variables out of version control
.env

# Turbo
.turbo
*/**/.turbo

# TypeScript incremental build cache
*.tsbuildinfo

# Playwright
test-results/
playwright-report/

# Claude Code session worktrees. .claude/launch.json is tracked on purpose:
# it is what `preview` uses to start the two dev servers.
.claude/worktrees/
