# Copy to .env.local for local development. Never commit .env.local.

# API base URL — points to the wordclimb Gin server.
# Leave unset to use the production default (https://wordclimb-api.fly.dev).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
